import Anthropic from '@anthropic-ai/sdk';
import { NextRequest, NextResponse } from 'next/server';

const client = new Anthropic();

export async function POST(req: NextRequest) {
  try {
    const { config, idea } = await req.json();

    const typeInstructions: Record<string, string> = {
      'checklist': `## チェックリスト形式の指示
- 各セクションにtypeを"checklist"で設定し、itemsに具体的なチェック項目を入れる
- 各項目は「〜する」「〜を確認する」等のアクション形式で、具体的に何をどうするかが分かる文にする
- 項目数は全体で40〜60個（各セクション5〜10個）
- セクション冒頭のcontentに、そのセクションの意図・なぜ重要か・どういう基準でチェックするかを3〜5行で説明
- 各チェック項目には必要に応じて補足説明を（）で付ける。例: 「SNSプロフィールにベネフィットを明記する（フォロー率が2倍になるデータあり）」
- 最後に「チェック結果の活用法」セクションを追加（チェック数ごとの診断と次のアクション）
- セクション数は7〜12セクション`,

      'guidebook': `## ガイドブック形式の指示
- 各セクションは800〜1,500文字の非常に充実した内容にする
- 各セクションに必ず以下を含む:
  - 具体的な事例（実際のBefore/After、成功事例）
  - 数字やデータ（統計、成果指標、期間）
  - ステップバイステップの手順（3〜5ステップ）
  - プロのコツ・裏技（「■ プロのポイント」で強調）
  - 各セクション末尾に「✅ このセクションのアクション」（3つ以上の具体的な行動）
- 「ステップ1→2→3」のように順番に実行できる構成
- 重要ポイントは「■ ポイント」で強調
- 随所に「例えば〜」「具体的には〜」を入れて抽象論を排除
- セクション数は8〜12セクション`,

      'video-script': `## 動画台本形式の指示
- 各セクションのtype を"script"で設定
- 台本は「話し言葉」で書く。読み上げてそのまま使えるレベル
- （間を置く）（画面に表示）（強調して）（視聴者に語りかけて）等の演出指示を含む
- 動画の長さは20〜30分想定（各セクション3〜5分）
- オープニング（つかみ30秒＋自己紹介＋この動画で得られること）→ 本編 → まとめ → CTA の構成
- セクション冒頭に【画面表示テキスト】を記載
- 視聴者への問いかけを各セクションに2つ以上入れる
- 具体的なエピソード・事例を各セクションに必ず含む
- セクション数は8〜12セクション`,

      'worksheet': `## ワークシート形式の指示
- 各セクションのtypeを"worksheet"で設定
- 各セクションの構成:
  - セクションの導入（なぜこのワークが重要か、2〜3行）
  - 質問群（3〜5問）→ 各質問に「記入例: 〇〇」を必ず付ける
  - ワークのヒント（「考えが浮かばない場合は〜」の補足）
  - ミニまとめ（このワークで分かったことの整理方法）
- 質問は具体的で答えやすいものから始め、徐々に深い問いへ
- 記入例は「例: 〇〇」の形式で、具体的で参考になる例を入れる
- 全体で30〜50の質問・記入項目
- 最後に「ワーク完了後の振り返りと次のステップ」セクションを追加
- セクション数は7〜10セクション`,

      'template': `## テンプレート集形式の指示
- そのままコピーして使える「型」を提供する
- 各テンプレートのセクション構成:
  - テンプレートの使い方（どんな場面で使うか、1〜2行）
  - テンプレート本文（＿＿＿で穴埋め箇所を示す。実用的で詳細なもの）
  - カスタマイズのポイント（itemsに3〜5個。「〇〇の部分は□□に変えると△△に使える」等）
  - 使用例（テンプレートを実際に埋めた完成例を1つ）
- 7〜12個のテンプレートを収録
- テンプレートは穴埋めするだけで完成する実用性を重視
- 各テンプレートは300〜500文字の充実した内容`,

      'swipe-file': `## スワイプファイル形式の指示
- 成功事例やそのまま使える素材を集めた形式
- 各事例のセクション構成:
  - 事例タイトル（キャッチーに）
  - 事例の詳細（何をしたか、具体的な施策、300〜500文字）
  - 成果・結果（数字やBefore/Afterで示す）
  - 「なぜうまくいったか」の解説（3〜5行の分析）
  - 「あなたの場合の応用方法」（itemsに3つ以上のアクション）
- カテゴリ分けして探しやすくする
- 12〜18個の事例・素材を収録
- セクション数は8〜12セクション`,
    };

    const prompt = `あなたはリードマグネット・特典コンテンツの制作専門家です。
以下のアイデアをもとに、見込み客が「これが無料？！」と感動する高品質な特典コンテンツの本文を生成してください。

## 商品情報
- 商品名: ${config.productName || '未設定'}
- 商品概要: ${config.productDescription || '未設定'}
- ターゲット: ${config.targetAudience || '未設定'}
- ターゲットの悩み: ${config.targetPain || '未設定'}
- ターゲットの理想: ${config.targetDesire || '未設定'}
${config.targetKnowledgeLevel ? `- ターゲットの知識レベル: ${{beginner:'初心者（専門用語は避け、一から丁寧に説明）',intermediate:'中級者（基礎は省略OK、実践テクニック中心）',advanced:'上級者（高度な戦略・最新トレンド中心）'}[config.targetKnowledgeLevel as string]}` : ''}
${config.targetUrgency ? `- ターゲットの緊急度: ${{low:'情報収集中（じっくり読む余裕あり、教育コンテンツ向き）',medium:'検討中（すぐ使えるノウハウが刺さる）',high:'今すぐ欲しい（即実践できるツール・テンプレートが最優先）'}[config.targetUrgency as string]}` : ''}
- 発信者の権威性: ${config.currentAuthority || '未設定'}
${config.conversionGoal ? `- 達成したい目標: ${config.conversionGoal}` : ''}
- 特典後のアクション: ${config.desiredAction || '未設定'}
- 用途: ${{list:'リスト獲得用',seminar:'セミナー参加特典',both:'リスト獲得+セミナー特典'}[config.funnelStage as string] || 'リードマグネット'}

## コンバージョン設計の指示
${config.funnelStage === 'list' ? `- はじめにで「この特典を受け取ったあなたは正解です」と登録を肯定する
- 各セクション末尾で「もっと深く知りたい方は…」のフックを自然に入れる（押し売り禁止）
- おわりにで「次のステップ」として有料商品の存在をさりげなく示唆
- CTA は「無料でここまで出すなら有料はどれほど…」と期待させる設計` :
config.funnelStage === 'seminar' ? `- はじめにで「セミナーでさらに深掘りする内容の予習として」と位置づける
- 本文中に2〜3箇所「この具体的な方法はセミナーで実演します」等の伏線を配置
- おわりにで「この特典の内容をセミナーで一緒に実践しましょう」と着席動機を作る
- CTA は「セミナーに参加して、今日学んだことを10倍加速させましょう」` :
`- リスト獲得とセミナー着席の両方を意識した導線設計
- 本文前半：特典単体で圧倒的な価値を感じさせる（リスト獲得力）
- 本文後半：セミナーへの期待を高める伏線を2〜3箇所配置（着席率UP）
- CTA：セミナー参加への自然な誘導`}
${config.conceptDesign ? `\n## コンセプト設計（ブランドの世界観・ポジショニング）\n${config.conceptDesign}\n→ 特典コンテンツはこのコンセプトのトーン・世界観と一貫させること` : ''}
${config.funnelDesign ? `\n## ファネル設計（集客〜成約の全体設計）\n${config.funnelDesign}\n→ 特典はこのファネルの中で「このステップ」を担う。前後の導線と整合させること` : ''}
${config.seminarContent ? `\n## セミナー内容（セミナーで話す内容）\n${config.seminarContent}\n→ 特典はセミナーの「予習」や「補足」の位置づけ。セミナーと内容が被りすぎないこと。セミナーへの期待を高める設計にすること` : ''}

## 選択されたアイデア
- タイトル: ${idea.title}
- 形式: ${idea.type}
- 概要: ${idea.description}
- 架け橋戦略: ${idea.bridgeStrategy}
- アウトライン: ${idea.outline.join(' / ')}

${typeInstructions[idea.type] || typeInstructions['guidebook']}

## 品質基準（すべて満たすこと）
### 定量的基準
- 全体で8,000〜15,000文字（動画台本は10,000〜18,000文字）
- セクション数: 8〜12セクション（少なすぎると薄い印象になる）
- 具体的なアクションアイテム: 20個以上
- 具体例・数字・事例: 10個以上
- データや統計の引用: 5個以上（一般的に知られている事実でOK）
- Before/After事例: 3個以上

### 定性的基準
- 明瞭さ: 専門用語を避け、ターゲットが一読で理解できる
- 即実践性: 読んだ直後に行動できる具体性
- 独自性: 「ググれば出てくる」レベルではない、プロならではの視点
- 感情インパクト: 「自分にもできそう」と希望を感じさせる
- 知覚価値: 「有料でも5,000円の価値がある」と思わせる充実度
- 架け橋効果: 読了後に「もっと学びたい」と有料商品への興味が高まる

## 回答形式（JSON）
以下のJSON形式で返してください。JSON以外の文字を含めないでください。
JSON内の文字列で改行が必要な場合は\\nを使うこと。

{
  "title": "特典タイトル",
  "subtitle": "サブタイトル（ターゲットへのメッセージ）",
  "type": "${idea.type}",
  "introduction": "はじめに（500〜800文字。読者への語りかけ、この特典で得られること、読み進める動機づけ）",
  "sections": [
    {
      "heading": "セクション見出し",
      "content": "セクション本文（600〜1,500文字。具体例・数字・手順を必ず含む充実した内容）",
      "type": "text",
      "items": ["アクションアイテムや補足リスト"]
    }
  ],
  "closingMessage": "おわりに（300〜500文字。感謝、学びのまとめ、次のステップへの期待）",
  "callToAction": "有料商品・セミナーへの自然な導線メッセージ（200〜400文字。具体的なベネフィットと行動喚起）"
}

## 絶対に守ること
- 有料商品の内容をすべて出し切らない。「一部を深く」出す
- 押し売り感を出さない。CTAは最後に1回だけ、自然な流れで
- 抽象論ではなく具体的なノウハウ・ツールを提供する
- 読者が「この人すごい」と権威性を感じる内容にする

## トーン・切り口の原則
- 「うまくいかない理由」「失敗する原因」等のネガティブ訴求は禁止
- 人は「なぜダメか」より「どうすればいいか」を知りたい
- 「より良い方法」「最短ルート」「プロのやり方」等のポジティブ訴求で書く
- 読者が「自分にもできそう！」とワクワクし、行動したくなるトーンにする
- 問題提起をする場合も、すぐに解決策・具体的な方法を提示すること`;

    let text = '';
    const stream = await client.messages.stream({
      model: 'claude-sonnet-4-20250514',
      max_tokens: 32768,
      messages: [{ role: 'user', content: prompt }],
    });
    const response = await stream.finalMessage();
    text = response.content[0].type === 'text' ? response.content[0].text : '';
    let jsonStr = text.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim();

    let parsed;
    try {
      parsed = JSON.parse(jsonStr);
    } catch {
      jsonStr = jsonStr
        .replace(/,\s*\{[^}]*$/, '')
        .replace(/,\s*"[^"]*$/, '')
        .replace(/,\s*$/, '');
      const openBraces = (jsonStr.match(/\{/g) || []).length - (jsonStr.match(/\}/g) || []).length;
      const openBrackets = (jsonStr.match(/\[/g) || []).length - (jsonStr.match(/\]/g) || []).length;
      jsonStr += ']'.repeat(Math.max(0, openBrackets)) + '}'.repeat(Math.max(0, openBraces));
      parsed = JSON.parse(jsonStr);
    }

    return NextResponse.json(parsed);
  } catch (error) {
    console.error('Generate content error:', error);
    return NextResponse.json(
      { error: error instanceof Error ? error.message : 'コンテンツ生成に失敗しました' },
      { status: 500 }
    );
  }
}
